import { NodeRefToIdPipe } from './node-ref-to-id.pipe';

describe('NodeRefToIdPipe', () => {
  it('create an instance', () => {
    const pipe = new NodeRefToIdPipe();
    expect(pipe).toBeTruthy();
  });
});
