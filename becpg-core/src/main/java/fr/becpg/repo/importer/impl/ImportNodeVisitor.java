/*
 * 
 */
package fr.becpg.repo.importer.impl;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

import fr.becpg.repo.importer.ImportVisitor;

/**
 * Class used to import a node with its attributes and files.
 *
 * @author querephi
 */
public class ImportNodeVisitor extends AbstractImportVisitor implements ImportVisitor {
	
	/** The logger. */
	private static Log logger = LogFactory.getLog(ImportNodeVisitor.class);
	
	/**
	 * Import a node 
	 */

}
