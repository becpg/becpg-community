#becpg-designer-share

module.id=${pom.artifactId}
module.title=${pom.name}
module.description=${pom.description}
module.version=${becpg.version}

module.repo.version.min=7.4

module.depends.becpg-share=${becpg.version}
